yfinance
pandas
numpy
python-dotenv
jsonschema
requests
aiohttp

# API + dashboard
fastapi
uvicorn
flask

# Cache (optional at runtime — falls back to an in-memory dict)
redis

# Market data / execution
fyers-apiv3
alpaca-trade-api

# LLM reasoning (all optional — deterministic fallback if absent)
google-genai
openai

# Tests
pytest
